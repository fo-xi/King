<Window x:Class="King.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:Controls="clr-namespace:King.Controls"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
		mc:Ignorable="d"
		Title="MainWindow" 
		Height="800" 
		Width="1200"
		ResizeMode="CanMinimize"
		WindowStartupLocation="CenterScreen">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding CloseWindowCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid x:Name="grid"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch">
        <Controls:RulesControl 
            DataContext="{Binding RulesControlVM}"/>
        <Controls:StartControl 
            DataContext="{Binding StartControlVM}"/>
        <Controls:WaitingConnectionControl 
            DataContext="{Binding WaitingConnectionControlVM}"/>
    </Grid>
</Window>
