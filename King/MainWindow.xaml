<Window x:Class="King.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:Controls="clr-namespace:King.Controls"
        xmlns:vms="clr-namespace:King.ViewModel;assembly=King.ViewModel"
		xmlns:local="clr-namespace:King"
		mc:Ignorable="d"
		Title="MainWindow" 
		Height="800" 
		Width="1200"
		ResizeMode="CanMinimize"
		WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vms:MainTabControlVM}">
            <Controls:MainTabControl/>
        </DataTemplate>
        
        <DataTemplate DataType="{x:Type vms:RulesControlVM}">
            <Controls:RulesControl/>
        </DataTemplate>
    </Window.Resources>

    <ContentControl
        Content="{Binding SelectedControl}"/>
</Window>
