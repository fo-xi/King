<Window x:Class="King.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:Controls="clr-namespace:King.Controls"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:Converters="clr-namespace:King.VisibilityConverters"
		xmlns:local="clr-namespace:King"
		mc:Ignorable="d"
		Title="MainWindow" 
		Height="800" 
		Width="1200"
		ResizeMode="CanMinimize"
		WindowStartupLocation="CenterScreen">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding CloseWindowCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch">
        <Controls:RulesControl 
            DataContext="{Binding RulesControlVM}"/>
        <Controls:MainTabControl 
            DataContext="{Binding MainTabControlVM}"/>
        <Controls:StartControl 
            DataContext="{Binding StartControlVM}"/>
    </Grid>
</Window>
